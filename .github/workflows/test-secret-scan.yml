name: Test · secret-scanning.yml

on:
  workflow_dispatch:
  push:
    branches: [ci-test-1]

permissions:
  contents: read

jobs:
  call:
    uses: ./.github/workflows/secret-scanning.yml
    secrets: inherit
    with:
      full-history: true
